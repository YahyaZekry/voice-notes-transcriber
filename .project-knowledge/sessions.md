# Session Log

> Part of voice-notes-transcriber/.project-knowledge/ | Last updated: 2026-08-10
> Append-only — never edit past entries.

| Date | Summary |
|------|---------|
| 2026-08-10 | Planned and built the transcriber from scratch: uv + Python 3.12 venv, `transcriber.py` (watch + `--once`, lazy model load, VAD, auto language, frontmatter notes, move-to-Completed), downloaded `large-v3` (~3 GB), validated end-to-end with synthetic clips (watch service picked up drops, English transcribed perfectly, Arabic detected), added startup catch-up scan, created `voice-transcriber.service` (auto-start at login). Moved project to `/mnt/airfryer/Projects/AI/voice-notes-transcriber/`, recreated venv, repointed service, initialized `.project-knowledge/`. |
| 2026-08-10 | Added desktop notifications (found / skipped / failed / complete, `notify-send` + `kdialog` fallback, `TRANSCRIBER_NOTIFY` toggle) and video support (`.mp4 .mkv .mov .avi .m4v .ts .3gp`, audio track transcribed). Hardened the stability gate (min 5s mtime age + size-idle, 10 min cap) after a KDE screen recording landed in the folder mid-recording; restricted watching to direct children. Validated end-to-end: a synthetic mp4 transcribed its audio correctly. git init'd the repo (`c6a5ea6`, branch `main`). |
| 2026-08-10 | Added speaker diarization (pyannote `speaker-diarization-3.1` + community embedding): installed CPU torch + pyannote in the venv, bypassed the broken `torchcodec` by feeding a PyAV-decoded waveform, fixed pyannote API drift (`token=`, `DiarizeOutput.speaker_diarization`), stored the user's HF token privately (`~/.config/voice-transcriber/hf.env`, 0600, wired via `EnvironmentFile`), and added `TRANSCRIBER_DIARIZE` / `TRANSCRIBER_SPEAKERS` toggles. Discovered a fundamental limit: diarization splits by voice identity, so two characters voiced by the same person are correctly kept as one speaker. |
| 2026-08-10 | Committed the notifications + video + diarization work (`b56a73d`, `7e41574`). User dropped a real 2-person Arabic video — diarization auto-mode collapsed it to 1 speaker (81/19 dominance). Diagnosed with a raw-pipeline probe, added auto-probe fallback (`_diarize_auto`: single-speaker result on ≥10s speech → probe `num_speakers=2`, accept only if minority ≥10% of speech AND ≥4s), restarted the service, reprocessed the video. Verified: note now labeled `speakers: 2` with alternating `**Speaker 1:**`/`**Speaker 2:**` turns. |
